# O*NET dumps (gitignored)

This directory is ignored by `/data/` in the repo `.gitignore`.

Put the **31.0 text** extract in `raw/`. Do not commit zips or TSVs.

Instructions, license, and the measured v1 freeze:

- `src/ta_taxonomies/suites/onet/README.md`
- `src/ta_taxonomies/suites/onet/INVENTORY.md`
